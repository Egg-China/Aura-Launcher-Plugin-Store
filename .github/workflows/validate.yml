name: Validate plugin store

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate registry schemas and security metadata
        shell: pwsh
        run: ./tools/validate-store.ps1
